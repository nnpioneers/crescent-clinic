# Global Rules

- After successfully completing a task or making code changes, ALWAYS run `git add`, `git commit -m "..."`, and `git push` automatically without waiting for the user to ask you to do it.

